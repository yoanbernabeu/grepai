# Example Docker Compose file for running Qdrant
# Run with: docker-compose -f docker-compose.example.yml up -d

services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  qdrant_data:
